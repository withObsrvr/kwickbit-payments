# Kwickbit Payment Indexer - Environment Variables
# Copy this file to .env and update with your values

# Database Configuration
DB_HOST=localhost
DB_PORT=5434
DB_PASSWORD=test
DB_DATABASE=kwickbit

# Google Pub/Sub Configuration
PUBSUB_PROJECT_ID=local-dev-project
PUBSUB_EMULATOR_HOST=localhost:8085

# For production with real GCP Pub/Sub:
# PUBSUB_PROJECT_ID=your-gcp-project-id
# GCLOUD_PUBSUB_PUBLISHER_SERVICE_ACCOUNT_KEY='{"type": "service_account", ...}'
# or use credentials file:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# and unset PUBSUB_EMULATOR_HOST

# Config File (for run.sh script)
CONFIG_FILE=config/kwickbit-payments-full.yaml
